PORT=5000
JWT_SECRET=campus360_super_secret_jwt_key_2026_production
JWT_EXPIRES_IN=7d

# MySQL Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=campus360_db

# Fallback mode: true means if MySQL is not running or connected, use seamless mock data store
ENABLE_AUTO_FALLBACK=true
